# Hold Note and Mobile Verification

プレビューでは、タイトル画面からプレイ画面へ遷移でき、プレイ画面に4レーン、長いシアンのバー、短い通常バー、オリジナルキャラクター、4つの入力パッドが表示された。バーは一点透視の消失点から判定ラインへ向かって拡大している。

入力パッドはPointer Eventsの`pointerdown`、`pointerup`、`pointercancel`に対応し、キーボードではkeydown/keyupで同じ処理を使う。したがって、iPadおよびスマートフォンのタッチ操作で、長押しバーが判定ラインに来たときに押し続け、終端付近で指を離す操作に対応する構造になっている。画面幅375pxのタイトル表示と、デスクトッププレビューのプレイ画面を確認した。

`pnpm check`と`pnpm build`は成功した。
